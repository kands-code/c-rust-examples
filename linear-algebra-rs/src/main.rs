use crate::linear_algebra::Vector::Vector::dot_product;

mod linear_algebra;

fn main() {
    let v1 = Vec::from([1, 2, 3]);
    let v2 = Vec::from([4, 5, 6]);
    let vec1 

    println!("{}", dot_product(v1, v2));
}
